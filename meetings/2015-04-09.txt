=> penser à expliquer l'installation de Glasir et d'ADTool, ça devrait marcher tel quel donc à tester sur une machine où y'a rien d'installé

=> penser à préciser quand Gildas n'est pas obligé de commenter ce qu'on envoie

FILTRE
pour les noeuds conjonctifs : garder uniquement ceux dont tous les fils respectent le filtre ?

ADTool : pour la commande faut changer MainWindow.java ligne (line 381). Il y a p'tetre des classes pour regarder les paramètres. La barre de menu est ligne 1044 dans mainwindow.java. Mettre en commentaire 990 et 993 pour enlever les barres de menu. Create Action est ligne 650 mais on sait pas trop ce qu'elle fait. On doit modifier toutes les classes avec Handler, pour les clics de souris ET les shortcuts.
Le zoom c'est dans mouseWheelEvent dans AbstractCanvasHandler. Le mouvement doit aussi etre la dedans. Y a aussi dans DomainCanvasHendler (pour les touches + et -). la fonction mouseDragged dans AbstCanvHand aussi sert donc faut pas la désactiver.

Copy paste : en fait remove node n'est pas utilisée, faut prendre removeTree. utiliser addChild() et addSiblings() dans ADTREECanvas, ou reprendre le même code pour faire une grosse fonction qui va faire le taf. pour mettre les noeuds correctement
=> labels, domains et fils du noeud où on colle à mettre à jour
Bon à priori, addChild fait tout le taf pour copypaste, mais dans ADTreeForGUI, y a childrenmap, parents et levels qui servent à définir un noeud (parents, frères, fils, et "frères de même niveau")


Pour le undo/redo, y a la stratégie de sauvegarder tout l'arbre à chaque action (les labels, les domains, tout ça) et rétablir l'arbre d'avant si annulation. En gros faut sauvegarder l'attribut tree de ADTreeCanvas, et l'attribut tree qui hérite de ADTreeCanvas dans DomainCanvas (pour sauver les domains).

